import ChannelPage from "./ChannelPage";

export default function Modules() {
  return (
    <ChannelPage
      channel="modules"
      title="Modules"
      subtitle="Module channels will show here (e.g. ADI632, PRG521)."
    />
  );
}
